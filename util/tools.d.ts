export declare namespace tools {
    function isEmptyObject(obj: any): boolean;
    function fillToLength(original: any, length: number): string;
    function getHash(indicator: string): number;
    function getIpAddress(req: any): any;
    function isErrorCountReset(first_time: any, minute: number): boolean;
}
